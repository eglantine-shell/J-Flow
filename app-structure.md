# 应用结构文档

本文档定义当前 V2.1 下的页面结构、主视图与用户路径。

说明：
- V2.1 当前处于 Todo 行为重构准备阶段
- 本轮只改文档，不改源码实现
- 若结构表述与旧实现冲突，以 `product-rules.md` 为准

---

## 一、整体结构

J-Flow 仍是单人、本地网页应用。

V2.1 的核心结构继续保持：
1. 首次初始化页
2. 主页面
3. 设置相关页面 / 弹窗
4. 种草管理相关页面 / 弹窗

其中主页面的主语必须明确为：
- Todo
- 种草

而不是：
- 模板管理
- 来源分支管理
- occurrence 管理

---

## 二、初始化页

### 目标

让用户建立两套轻量认知：
- 自己会积累哪些种草
- 自己的“有空”通常长什么样

### 内容

1. 欢迎说明
2. 种草清单默认分类设置
3. 有空就做场景设置
4. 完成并进入应用

### 交互要求

- 系统提供默认项
- 用户可新增
- 用户可删除
- 至少保留一个分类和一个场景后才能进入主页面

---

## 三、主页面

V2.1 主页面继续保持四个主要区域：
1. 头部
2. 日期
3. Todo
4. 种草

其中 Todo 必须是主页面第一主语。

这意味着：
- 用户进入页面时首先感知到的是“今天有哪些 Todo”
- 用户做的主要动作是“新增 / 完成 / 删除 / 编辑 Todo”
- 用户不应先被迫理解模板、来源、实例、重复实例

---

## 四、头部

### 展示内容

- 应用名称
- 设置入口
- 必要的辅助状态入口

### 目标

- 保持轻量
- 不抢占 Todo 主体

---

## 五、日期区

### 展示内容

- 当前选中日期
- 星期信息
- 日期切换能力

### 目标

- 日期是 Todo 的上下文锚点
- 用户感知到的是“查看某一天的 Todo”
- 日期切换不应暴露底层自动生成或实例链心智

---

## 六、Todo 区

### 页面目标

Todo 区是主页面核心，用于承载某一天真实要做的事。

### 用户可见内容

- 普通 Todo
- 从种草加入的 Todo
- 重复 Todo 的当前可感知实例
- 未完成后延续到今天的 Todo

### 用户可见行为

Todo 区首先应支持成熟 Todo List 直觉下的基础操作：
- 快速新增
- 编辑标题
- 完成
- 取消完成
- 删除
- 查看准备备注
- 推进分次进度

### 添加入口方向

Todo 添加入口继续维持一个统一入口：
- 默认快速记一条 Todo
- 按需展开更多选项

展开后可配置：
- 从种草添加
- 必要标记
- 重复规则
- 需要准备
- 分次进度能力

### Todo 区的 V2.1 结构要求

后续实现中，Todo 区不应直接消费底层 `DayPlanItem` 并按内部字段分叉交互。

Todo 区应基于 `TodoViewModel` 工作：
- UI 只关心显示什么
- UI 只关心按钮可不可点
- 映射层负责吸收底层差异

V2.1-2 当前已完成第一步收口：
- `TodoModePanel` 已先将当天 `DayPlanItem` 映射为 `TodoViewModel`
- 编辑、删除、完成、取消完成、停止重复等按钮状态优先读取 ViewModel 能力字段
- 底层 `source / templateKind / recurringInstanceId` 等差异尽量收口到映射层

V2.1-3 当前已完成第二步：
- Todo 日期加载顺序改为：
  1. `syncAutoGeneratedDayPlanForDate`
  2. `syncTodoCarryoversForDate`
  3. 读取并映射 `DayPlanItem`
- 普通未完成 Todo 与分次 Todo 现在共用同一套 carryover 同步机制
- `continuation` 在结构层不再表达为“分次专属模块”，而是 todo carryover 的内部机制

V2.1-4 当前推荐的下一步结构是：
- repeating Todo 也进入“active occurrence + carryover”的同一产品心智
- 但其底层同步顺序仍保持：
  1. 先由 recurrence 判断今天是否应生成新的 occurrence
  2. 再由 carryover 判断旧 occurrence 是否应继续显示到今天
- UI 层继续只显示一条当前待处理的重复 Todo，而不是多条同模板堆叠实例

V2.1-4 当前已实现第一版：
- `auto-generated` 在生成 repeating Todo 前，会先判断该模板是否已有 active pending occurrence
- `todo-carryover` 已支持把未完成 repeating occurrence 延续到新日期

### Todo 区的来源展示

若需要展示来源，应只作为轻量辅助标签，例如：
- 手动添加
- 来自种草
- 重复
- 延续自昨天

这些标签不应决定：
- 是否可编辑
- 是否可删除
- 是否可完成
- 是否可取消完成

---

## 七、种草区

### 页面目标

种草区承载的是“先记住，以后有空再做”的内容。

### 页面职责

- 展示种草条目列表
- 展示种草分类
- 作为 Todo 的可选输入来源
- 提供拔草入口

### 结构边界

种草区不是普通 Todo 的前置流程。

这意味着：
- 用户可以完全不经过种草，直接使用 Todo
- 从种草加入后，用户感知到的仍是普通 Todo
- Todo 的编辑、完成、删除、跨日延续，不回退成“模板管理行为”

---

## 八、拔草推荐

### 页面目标

当用户有空时，从种草中帮助其挑选适合当前语境的内容。

### 表达方式

V2.1 中，拔草推荐更适合继续作为：
- Todo 添加区中的一种动作
- 或种草区进入 Todo 的辅助入口

它不是独立主模式。

### 结构边界

拔草推荐只负责“选哪条加入 Todo”，不负责定义：
- Todo 的完成语义
- Todo 的删除语义
- Todo 的跨日延续语义

---

## 九、设置页 / 设置弹窗

V2.1 暂时仍保留：
- 分类管理
- 场景管理
- 排序或偏好设置
- 数据测试与重置入口

本轮不改这些结构。

---

## 十、种草管理页 / 详情弹窗

后续仍可保留种草管理能力，例如：
- 查看已有种草
- 编辑种草
- 停用种草
- 查看准备、重复、分次等属性

但这些属于“种草管理”，不是“Todo 行为定义”。

用户进入 Todo 区时，不应被迫理解这些管理对象。

---

## 十一、TodoViewModel 结构规划

### 1. 目标

在 UI 层引入统一的 `TodoViewModel`。

### 2. UI 层只消费以下语义

- `id`
- `title`
- `date`
- `timeBlock`
- `isCompleted`
- `isDeleted`
- `isSegmented`
- `progressPercent`
- `isRepeating`
- `isNecessary`
- `preparationNotes`
- `carryHint`
- `originLabel`
- `canEdit`
- `canDelete`
- `canComplete`
- `canUncomplete`
- `canStopRepeating`
- `internalRef`

### 3. UI 层不应直接依赖

- `source`
- `templateKind`
- `recurringInstanceId`
- `consumesDateTrigger`
- `continuationOfItemId`
- `rootItemId`

这些字段可以继续存在于底层，但不应直接决定 UI 行为。

---

## 十二、内部实现层说明

以下概念在结构文档中保留，但降级为内部实现层：
- 模板层
- 重复实例层
- 当日实例层
- continuation / carryover 链路

它们的职责是：
- 保证数据可存储
- 保证重复与延续可同步
- 保证历史兼容

它们不应成为用户在主页面上直接感知的结构主语。

---

## 十三、当前不做

本轮只更新结构文档，不做：
- UI 实现
- 路由调整
- 页面重构
- 源码改造
- schema 变更
