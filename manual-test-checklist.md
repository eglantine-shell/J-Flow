# V2.1 手动验证清单

当前版本目标：为 Todo 行为重构后的手动验证提供口径。

说明：
- 本轮只更新手测规则，不代表这些能力已全部实现
- 若实现与本文档暂时不一致，以 `product-rules.md` 作为后续重构目标

## 1. 初始化流程
- 清空本地数据后首次进入应用，应先看到初始化页。
- 页面标题应为：`哪怕你是一个100%的J人，想必也会有……`
- 默认种草清单应为：`阅读`、`观影`、`旅游`
- 默认有空就做应为：`工作日晚上`、`周末`、`长假`
- 两组 tag 都支持原位新增和删除。
- 每组至少保留一个 tag，删除到只剩一个时应被阻止。
- 输入空白内容时不能保存。
- 完成初始化后应进入主页。

## 2. 普通 Todo 基础行为
- 在主页 Todo 顶部输入一条文本。
- 选择白天或晚上。
- 不展开“更多”，直接点击加号图标。
- 应创建一条普通 Todo。
- 新条目应可编辑标题。
- 新条目应可勾选完成。
- 完成后标题应加删除线，条目内容透明度应降低。
- 已完成 Todo 应可取消完成。
- 新条目应可删除。

## 3. 普通未完成 Todo 顺延到今天
- 创建一条普通一次性 Todo。
- 不完成它，在真实时间尚未过零点时手动切到第二天。
- 这条 Todo 不应因为手动查看未来日期而提前出现。
- 真实时间过零点后进入当天视图，这条 Todo 应继续出现。
- 且昨天不应再保留一条仍然 pending 的独立副本。
- 若第二天仍未完成，切到第三天时，这条 Todo 应继续被顺延到第三天。
- 完成它后，后续日期不应继续出现。
- 删除它后，后续日期不应继续出现。
- 若这条 Todo 不是今天创建的，应显示“创建于 M/D”而不是“延续自 M/D”。
- 顺延后的 Todo 应继续可编辑、可删除、可完成、可取消完成。
- 若一条 Todo 已经 `completed` 或 `deleted`，后续日期不应再继续顺延。

## 4. 从种草加入后的 Todo 行为
- 在 Todo 添加区展开“更多”。
- 从种草中加入一条 Todo。
- 加入后，这条 Todo 应按普通 Todo 行为运行。
- 应可编辑标题。
- 应可完成。
- 应可取消完成。
- 应可删除。
- 未完成时应可顺延到今天。
- 编辑当天 Todo 不应回写原种草标题。
- 删除当天 Todo 不应删除原种草。

## 5. 必要 与 需要准备
- 在“更多”里勾选“必要事项”创建 Todo。
- 新条目应展示必要标记。
- 该标记不应改变完成、删除、顺延到今天的基础行为。
- 在“更多”里勾选“需要准备”，输入准备备注。
- 创建后应展示单行灰字：`前置准备内容：xxxx`
- 准备备注不应改变排序、完成、删除、顺延到今天的基础行为。

## 6. 分次 Todo 作为进度属性
- 在“更多”里勾选“分次事项”创建 Todo。
- 条目应显示进度条，且位于事项内容下方。
- 若保持 `0%` 且不完成，切到第二天时也应继续出现。
- 推进到 `1-99%` 后，应仍视为未完成。
- 切到第二天时，这条 Todo 应继续顺延到今天，并保持进度。
- 推进到 `100%` 后，应视为完成。
- 分次进度条应可直接拖动并立即保存，不需要“推进”或“保存进度”按钮。
- 分次进度区不应再显示当前进度文字、百分比文字或单独的“已完成”提示。
- 到后续日期不应再继续出现。
- 分次 Todo 的删除语义应与普通 Todo 一致：删除后结束整条 Todo。
- 已完成的分次 Todo 应可取消完成。
- 取消完成后应回到：
  - `pending`
  - `in_progress`
  - `90%`
- 再次拖到 `100%` 后，应再次自动完成。

## 7. 重复 Todo 行为
- 创建一条带重复规则的 Todo。
- 当天应出现当前 Todo。
- 若删除当天实例，则同一天不应再出现该实例。
- 删除当天实例不应停止未来重复。
- 若执行“停止重复”，未来日期不应再出现新的重复 Todo。
- 若某个 occurrence 未完成，真实日期推进后，它应自己顺延到今天。
- 到下一个命中日时，新的 occurrence 仍应正常出现。
- 因此同模板 repeating Todo 在今天可能并存多条，这不是 bug。
- 并存的 repeating Todo 应靠“创建于 M/D”区分。
- 删除某条 repeating occurrence 应只结束这一条，不停止整条重复规则。
- “停止重复”后，未来命中日不应再生成新的 occurrence，但历史 occurrence 应保留。
- 同一模板 + 同一命中日，不应重复生成第二条 occurrence。
- 当日 repeating Todo 打勾完成后，应仍留在当天列表中显示为已完成，不应在刷新或同步后消失。
- 停止重复后，当天 repeating Todo 无论是未完成、已完成，还是已完成后再取消完成，都应继续保留在当天列表；只有未来命中日不再生成新的 occurrence。
- 昨日未完成的 daily repeating Todo 顺延到今天后，昨天视图中不应再残留这条 pending occurrence；历史同步不应把它从今天拉回昨天。
- 查看过去日期时，不应渲染新增 Todo 区；历史 Todo 列表仍可查看。
- 若今天同时存在“今天命中的 repeating occurrence”和“过去搬移到今天的 occurrence”，只有今天命中的那条显示循环图标按钮。
- 点击循环图标后，应停止未来重复；图标应切换为带斜线的恢复态；再次点击应恢复重复，未来命中日重新生成。
- repeating Todo 应支持直接编辑当前 occurrence 标题。
- 编辑当前 repeating Todo 标题后，只应改当前 Todo，不应回写模板，也不应影响未来 occurrence。

## 8. UI 交互一致性
- Todo 列表中的编辑、完成、取消完成、删除按钮，不应因为来源不同而出现难理解差异。
- 手动输入、从种草加入、重复生成、顺延到今天后的 Todo，都应保持一致的基础交互直觉。
- 若展示来源标签，它应只作为辅助信息，不应决定按钮是否可用。
- Todo 列表不应再显示“白天 / 晚上”文字标题，但仍应通过背景色区分白天和晚上条目。
- Todo 条目不应再显示：
  - `手动`
  - `种草`
  - `重复`
  - `准备`
  - `分次 xx%`
- 完成事项应优先显示 `完成于 MM/DD HH:mm`；若没有 `completedAt`，则回退显示 `已完成`。

## 8A. TodoViewModel 收口
- 日期提示应从“延续自”改成“创建于”。
- `createdAtHint` 应稳定读取 `originDate`，旧数据按兼容规则回退。
- 编辑按钮显示应基于统一能力判断，不应因为 JSX 中散落的来源分支而失效。
- 删除按钮显示与禁用状态应基于统一能力判断。
- 完成 / 取消完成按钮禁用状态应基于统一能力判断。
- “停止重复 / 恢复重复”入口应继续只出现在当前支持的重复 Todo 上。

## 9. 种草区边界
- 种草区应继续用于保存“以后有空再做”的内容。
- 用户无需先种草，仍可直接使用 Todo。
- 从种草加入后，用户感知到的是普通 Todo，而不是模板实例管理。
- 拔草推荐应只保留一个切换按钮：
  - 未展开时显示 `打开拔草推荐`
  - 已展开时显示 `收起拔草推荐`
- 拔草推荐内不应再显示“白天 / 晚上”文案。
- 拔草推荐里的“种草清单”切换应使用 tag，而不是 `select`。

## 10. 构建与 lint
- 实现轮应执行 `pnpm run build`
- 实现轮应执行 `pnpm run lint`

## 当前明确未覆盖能力
- `decision_selected` 转重复
- 拖动排序 / 拖动改变晚间语境
- 导出 / 导入
- 已归档种草恢复
- 搜索 / 筛选
- 自动化业务测试
- iOS App 迁移
