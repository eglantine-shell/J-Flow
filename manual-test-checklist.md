# V2.1 手动验证清单

当前版本目标：
- `V2.0`
  - 已完成本地文件夹同步能力验证
- `V2.1`
  - 在 `V2.0` 基础上补充必要事项 `DDL` 相关验证

说明：
- 本轮已更新“种草 / 拔草”目标规则，不代表这些能力已全部实现
- 若实现与本文档暂时不一致，以 `product-rules.md` 作为后续重构目标

## 1. 初始化流程
- 清空本地数据后首次进入应用，应先看到初始化页。
- 页面标题应为：`哪怕你是一个100%的J人，想必也会有……`
- 默认种草清单应存在并可编辑。
- 默认“有空就做”场景应存在并可编辑。
- 两组 tag 都支持原位新增和删除。
- 每组至少保留一个 tag，删除到只剩一个时应被阻止。
- 输入空白内容时不能保存。
- 完成初始化后应进入主页。
- 初始化页不应暗示场景会自动映射白天 / 晚上或周中 / 周末。

## 2. 普通 Todo 基础行为
- Todo 输入区第一行应显示两组 segmented control：
  - `普通条目 / 拔草条目`
  - 白天 / 晚上图标
- 同一行还应显示一个日期入口（日历 icon）。
- 两组 segmented control 应保持在同一行：
  - 左侧主
  - 右侧辅
- 日期入口、模式切换、日夜切换三者应尽量保持在同一行。
- 两组 segmented control 的高度应一致。
- 若日期入口高度更高，其余两个切换组件应同步对齐到相同高度。
- 在主页 Todo 顶部输入一条文本。
- 选择白天或晚上。
- 不展开“更多”，直接点击输入框内右侧加号图标。
- 应创建一条普通 Todo。
- quick add 时可通过日历入口把 Todo 直接创建到未来日期。
- 若 quick add 时把日期改到未来，这条 Todo 不应继续留在当前日期列表里。
- 新条目应可编辑标题。
- 新条目应可勾选完成。
- 已完成 Todo 应可取消完成。
- 新条目应可删除。
- 输入框外不应再显示单独的大号添加按钮。

## 3. 普通未完成 Todo 顺延到今天
- 创建一条普通一次性 Todo。
- 不完成它，在真实时间尚未过零点时手动切到第二天。
- 这条 Todo 不应因为手动查看未来日期而提前出现。
- 真实时间过零点后进入当天视图，这条 Todo 应继续出现。
- 且昨天不应再保留一条仍然 pending 的独立副本。
- 若第二天仍未完成，切到第三天时，这条 Todo 应继续被顺延到第三天。
- 完成它后，后续日期不应继续出现。
- 删除它后，后续日期不应继续出现。
- 若这条 Todo 不是今天创建的，应显示“创建于 M/D”而不是“延续自 M/D”。

## 3A. 普通 Todo 改计划日期
- 在未完成 Todo 上点击编辑。
- 编辑浮层第一行应显示日历 icon，且与模式切换、日夜切换同一行。
- 将今天的一条未完成 Todo 改到未来某天，例如下个周末。
- 保存后，这条 Todo 应立即从今天列表消失。
- 中间那些天不应继续自动顺延显示这条 Todo。
- 到了新日期后，这条 Todo 应出现在新日期的未完成区。
- 如果新日期当天仍未完成，后续再按现有顺延规则继续运行。
- 已完成事项不应在这个编辑浮层里开放改计划日期。

## 9A. 重复 Todo 改计划日期
- 选择一条未完成的 repeating occurrence，进入编辑。
- 通过日历入口把它改到未来某天。
- 保存后，应只改当前 occurrence 的显示日期。
- 原重复规则本身不应被改写。
- 之后未来命中日的重复生成仍应按原规则继续。

## 4. 种草基础行为
- 新增一条种草时，只应编辑：
  - 种草清单
  - 标题
  - 场景
  - 兴趣程度
- 种草内容输入应为单行 input，而不是多行 textarea 视觉。
- 种草标题右侧工具区应显示：
  - 展开 / 收起输入区
  - 保存 icon
  - 展开 / 收起种草列表
- 顶层卡片标题应为：`种草`
- 第一个展开 / 收起按钮应为：`+ / -`
- 第三个按钮应为三横线，用于展开 / 收起 `种草清单`
- 在输入为空时，保存 icon 应 disabled / 弱化。
- 在输入有内容时，保存 icon 应可点击保存。
- 新增种草时，不应再要求或暴露：
  - 必要
  - 重复
  - 准备
  - 分次
  - 日期
- 管理种草时，不应再出现完整编辑表单。
- 停用种草后，它不应再参与未来拔草候选。
- 停用种草不应删除已有 Todo。

## 4A. 种草生命周期
- 新建后的种草默认应处于可见、可拔草状态。
- 从种草加入一条一次性 Todo 后，这条种草应离开活动种草库。
- 已被加入 Todo 的种草不应继续出现在拔草候选中。
- 来自种草的一次性 Todo 若被删除，对应种草应回到活动种草库。
- 来自种草的一次性 Todo 若被完成，对应种草不应回到活动种草库。
- 用户主动停用种草，与“已被加入 Todo”应是两个不同状态。
- 当前版本虽然主要只展示 `active` 种草，但底层状态仍应能稳定区分：
  - `active`
  - `picked`
  - `archived`

## 4B. 种草列表 UI
- 列表面板标题应为：`种草清单`
- 展开种草列表后，不应再显示：
  - `Grass / x 条种草`
  - `选择一条种草开始编辑`
  - 编辑按钮
  - `x 个场景` 或 active 状态 tag
- 种草列表顶部应支持：
  - 种草清单 tag 单选
  - 场景 tag 多选
- 未选择场景 tag 时，不应按场景过滤。
- 每条种草应只保留：
  - 标题
  - 兴趣 stepper
  - 停用 `×`
- 兴趣 stepper 应支持：
  - `-`
  - 当前兴趣值
  - `+`
- 当前版本兴趣最小值应为 `1`，最大值应为 `3`。
- 当前版本种草列表默认只展示 `active`，这属于已知阶段性限制。
- 每条种草应支持直接编辑条目内容。
- 该编辑只应修改条目标题，不应改动清单 tag 或场景 tag。
- 编辑后应立即在列表中显示新内容。
- 删除种草时，确认文案应为：
  - `确认永久删除种草条目「xx」吗？`

## 4C. 日志页（Logbook）
- Sidebar 中原“垃圾桶”入口应改为：`日志`。
- 进入日志页后，应看到按日期倒序排列的日志卡片。
- 每张日志卡片应包含：
  - 当日完成
  - 当日未完成
  - 当日删除
  - 备注
- 当日完成区中：
  - 普通完成应显示 `完成`
  - 来源于种草且完成的事项应显示 `拔草`
- 必要事项在“当日完成”中应显示为加粗标题。
- 若某条未完成事项是分次事项，其标题后应追加：
  - `进度：x%`
- 若某条分次事项当天被推进过，且当天结束时仍未完成，则 `当日未完成` 应优先显示：
  - `推进 xxx 20% -> 40%`
- 不应额外新增独立的“当日推进”区块。
- 手动改到未来日期的事项，不应出现在原日期的“当日未完成”中。
- 删除 Todo 后，次日进入应用时，该事项应出现在前一天的“当日删除”中。
- 备注区应可编辑并保存。
- 点击“复制 Markdown”后，应能粘贴出完整日志 Markdown 文本。
- 日志正文中的完成 / 未完成 / 删除快照不应直接在页内改写。
- 每条日志卡片头部应显示一个轻量 `删除` 按钮。
- 点击删除后，应先提示是否永久删除该条当日快照。
- 确认后，这条日志应立刻从列表消失。
- 删除日志不应反向影响原始 Todo 数据。

## 5. 从种草加入后的 Todo 行为
- 在 Todo 输入区顶部切换到 `拔草条目`。
- 普通输入框、输入框内右侧加号、`...` 更多按钮仍应继续显示。
- 选择一个种草清单。
- 不选择场景时，应能看到该清单下全部候选。
- 选择一个或多个场景后，应只看到 `sceneTagIds` 有交集的候选。
- 切回 `普通条目` 后，拔草面板应收起。
- 从种草中加入一条 Todo 后，这条 Todo 应按普通 Todo 行为运行。
- 应可编辑标题。
- 应可完成。
- 应可取消完成。
- 应可删除。
- 未完成时应可顺延到今天。
- 编辑当天 Todo 不应回写原种草标题。
- 删除当天 Todo 不应删除原种草。
- 完成当天 Todo 不应影响原种草。
- 删除来自种草的一次性 Todo 时，应恢复原种草到活动库。
- 完成来自种草的一次性 Todo 时，不应恢复原种草。
- 若来自种草的 Todo 在删除前已顺延到后一天，删除时仍应按当前规则恢复或不恢复原种草。

## 6. 场景筛选行为
- 系统不应再根据日期自动推断周中 / 周末场景。
- 系统不应再根据白天 / 晚上自动推断场景。
- 系统不应再根据 tag 中文名称做语义匹配。
- 场景筛选只应按用户当前选中的 `sceneTagId` 精确匹配。
- 未选场景时，不应因为模板场景为空或不为空而改变排序权重。
- 场景筛选 UI 应使用 tag 多选，不应再使用独立打开 / 刷新按钮。

## 7. 拔草排序与去重
- 同一清单下，兴趣更高的候选应排在前面。
- 兴趣相同时，应按 `tieBreakerOrder` 控制创建时间先后。
- 当天已经加入 Todo 的同种草项，不应继续作为默认推荐排在最前。
- 同一天同一时段，不应允许重复添加同一条种草。
- 是否允许“同一天另一时段再次添加同一条种草”，需按后续确认规则验证。
- 拔草候选列表不应再显示“默认推荐”“候选列表”“兴趣 1/2/3”等额外说明文案。
- 每条候选应以标题为主，右侧用加号图标执行添加。
- 移动端下，候选标题和右侧黄色加号仍应保持同一行。

## 8. 必要、准备、分次、重复的归属
- 这些属性不应再属于“种草编辑”。
- 它们应只在“加入 Todo”或“直接创建 Todo”时设置。
- 从种草加入 Todo 后，这些属性应写在当前 Todo 上，而不是回写到种草模板。
- 在普通条目模式下，必要 / 重复 / 准备 / 分次应使用统一视觉系统：
  - 高度一致
  - 圆角一致
  - padding 一致
  - 重复规则不应再显得像突兀的原生 select
- 更多设置区应是紧凑附加设置区，而不是大卡片式第二主界面。
- 第一行应为：
  - `必要`
  - `准备`
  - `分次`
- 第二行应为：
  - `不重复`
  - `每天`
  - `每周`
  - `每月`
  - `每年`
- 移动端下也应保持这两行结构，不应把第一行三项改成竖排。
- 移动端下重复选项不应被压成五个小竖条；应保持单行可读选项，必要时允许横向滑动。
- 移动端下重复选项的横向滚动范围应覆盖整排，不应只多出单个小选项的长度。

## 9. 重复 Todo 行为
- 创建一条带重复规则的 Todo。
- 当天应出现当前 Todo。
- 若删除当天实例，则同一天不应再出现该实例。
- 删除当天实例不应停止未来重复。
- 若执行“停止重复”，未来日期不应再出现新的重复 Todo。
- 若某个 occurrence 未完成，真实日期推进后，它应自己顺延到今天。
- 到下一个命中日时，新的 occurrence 仍应正常出现。
- 因此同模板 repeating Todo 在今天可能并存多条，这不是 bug。
- 并存的 repeating Todo 应靠“创建于 M/D”区分。
- 若来自种草的 Todo 被转成 repeating Todo，删除某次 occurrence 或停止重复都不应自动恢复原种草。
- 若某个 repeating occurrence 未完成并被顺延，下一次命中日新生成的 occurrence 仍应继续出现。
- 若顺延中的旧 occurrence 与新命中日 occurrence 同时存在，它们都应可独立完成 / 删除。
- 删除旧顺延 occurrence，不应影响新命中日 occurrence。
- 停止重复后：
  - 历史已存在 occurrence 仍保留
  - 已顺延中的 occurrence 仍保留
  - 未来命中日不再新生成
- 若恢复重复，应只影响未来命中日，不应重建已删除的历史 occurrence。

## 10. UI 交互一致性
- Todo 列表中的编辑、完成、取消完成、删除按钮，不应因为来源不同而出现难理解差异。
- 手动输入、从种草加入、重复生成、顺延到今天后的 Todo，都应保持一致的基础交互直觉。
- 若展示来源标签，它应只作为辅助信息，不应决定按钮是否可用。
- 白天事项与晚上事项可以保留不同背景，但中间不应再有额外实线分隔。

## 10A. 完成时间取整
- 设置页应新增“完成时间取整”设置。
- 可选值应为：
  - `不取整`
  - `5 分钟取整`
  - `10 分钟取整`
  - `30 分钟取整`
- 默认值应为：`5 分钟取整`。
- 当某条 Todo 在 `16:36` 被勾选完成时：
  - 选择 `不取整`，应记录为 `16:36`
  - 选择 `5 分钟取整`，应记录为 `16:35`
  - 选择 `10 分钟取整`，应记录为 `16:40`
  - 选择 `30 分钟取整`，应记录为 `16:30`
- 修改完成时间输入框时，仍应按用户输入值保存，不应二次自动取整。

## 11. 构建与 lint
- 实现轮应执行 `pnpm run build`
- 实现轮应执行 `pnpm run lint`

## 12. 网页图标
- 浏览器标签页应显示 `logo.PNG` 作为网页图标。

## 13. 删除 / 重置保护
- 删除普通 Todo 前，应出现确认。
- 删除 repeating occurrence 前，用户应能理解这是“只删除本次”，而不是停止未来重复。
- 停止重复前，应出现确认，且文案应强调“未来不再自动出现，历史保留”。
- 停用种草前，应出现确认。
- 设置页中的“重置应用（测试用）”前，应出现确认。
- 重置后应回到初始化页，且本地数据被清空。

## 14. 数据安全闭环
- 导出后应得到可保存的本地数据文件。
- 新环境导入后，应恢复：
  - Todo
  - repeating 规则
  - 种草
  - 清单与场景标签
  - 设置项
- 导入失败时，应给出明确错误，不应半写入半损坏。
- 导入前若已有本地数据，应有明确保护提示。
- 导入后应重点回归：
  - `originDate`
  - `grassStatus`
  - repeating occurrence 行为
  - picked / archived 是否仍符合预期
- 设置页中“数据导入 / 导出”入口应位于：
  - 排序设置之后
  - 测试工具之前
- 当前导入语义应明确为：
  - 用备份文件整体覆盖当前本地数据
  - 不做合并导入

## 15. macOS 打包前验收
- `pnpm run build:desktop` 应通过。
- Electron 开发窗口应能正常打开主页，不应出现 404 或初始化状态读取失败。
- 设置页应能显示：
  - 当前桌面数据目录
  - 当前 SQLite 主库文件
  - 自动备份目录
- 设置页应能成功导出 JSON 备份文件。
- 设置页应能成功导入 JSON 备份文件，并在导入后恢复设置与数据。
- 设置页应能手动创建自动备份。
- 创建或导入一些数据后，关闭并重新打开 Electron，SQLite 数据应仍能读回。
- 数据目录中的运行时主库文件应为：
  - `j-flow.sqlite3`
- 自动备份目录应位于：
  - 数据目录下 `backups/`

## 16. macOS `.dmg` 第一版验收
- `pnpm run package:mac` 应生成 `.dmg` 产物。
- 挂载 `.dmg` 后应能看到 `J-Flow.app`。
- 将应用拖入 `Applications` 后应能正常启动。
- 首次启动后应能进入主页，而不是白屏、404 或 bridge 报错。
- 安装后的应用应继续能够：
  - 显示数据目录与主库文件
  - 导出 JSON
  - 导入 JSON
  - 创建自动备份

## 当前明确未覆盖能力
- `decision_selected` 转重复
- 拖动排序 / 拖动改变晚间语境
- 搜索 / 筛选（待评估是否在网页端补）
- 自动化业务测试
- iOS App 迁移
