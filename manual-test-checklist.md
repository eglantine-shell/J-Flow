# V2.1 手动验证清单

当前版本目标：为 Todo 行为重构后的手动验证提供口径。

说明：
- 本轮只更新手测规则，不代表这些能力已全部实现
- 若实现与本文档暂时不一致，以 `product-rules.md` 作为后续重构目标

## 1. 初始化流程
- 清空本地数据后首次进入应用，应先看到初始化页。
- 页面标题应为：`哪怕你是一个100%的J人，想必也会有……`
- 默认种草清单应为：`阅读`、`观影`、`旅游`
- 默认有空就做应为：`工作日晚上`、`周末`、`长假`
- 两组 tag 都支持原位新增和删除。
- 每组至少保留一个 tag，删除到只剩一个时应被阻止。
- 输入空白内容时不能保存。
- 完成初始化后应进入主页。

## 2. 普通 Todo 基础行为
- 在主页 Todo 顶部输入一条文本。
- 选择白天或晚上。
- 不展开“更多”，直接点击 `+`。
- 应创建一条普通 Todo。
- 新条目应可编辑标题。
- 新条目应可勾选完成。
- 完成后标题应加删除线，条目内容透明度应降低。
- 已完成 Todo 应可取消完成。
- 新条目应可删除。

## 3. 普通未完成 Todo 跨日延续
- 创建一条普通一次性 Todo。
- 不完成它，切到第二天。
- 这条 Todo 应继续出现。
- 若第二天仍未完成，切到第三天仍应继续出现。
- 完成它后，后续日期不应继续出现。
- 删除它后，后续日期不应继续出现。
- 若第二天出现的是 carryover Todo，应显示“延续自 M/D”提示。
- carryover Todo 应继续可编辑、可删除、可完成、可取消完成。

## 4. 从种草加入后的 Todo 行为
- 在 Todo 添加区展开“更多”。
- 从种草中加入一条 Todo。
- 加入后，这条 Todo 应按普通 Todo 行为运行。
- 应可编辑标题。
- 应可完成。
- 应可取消完成。
- 应可删除。
- 未完成时应可跨日延续。
- 编辑当天 Todo 不应回写原种草标题。
- 删除当天 Todo 不应删除原种草。

## 5. 必要 与 需要准备
- 在“更多”里勾选“必要事项”创建 Todo。
- 新条目应展示必要标记。
- 该标记不应改变完成、删除、跨日延续的基础行为。
- 在“更多”里勾选“需要准备”，输入准备备注。
- 创建后应展示备注文本。
- 准备备注不应改变排序、完成、删除、跨日延续的基础行为。

## 6. 分次 Todo 作为进度属性
- 在“更多”里勾选“分次事项”创建 Todo。
- 条目应显示进度条与百分比。
- 若保持 `0%` 且不完成，切到第二天时也应继续出现。
- 推进到 `1-99%` 后，应仍视为未完成。
- 切到第二天时，这条 Todo 应继续出现，并保持进度。
- 推进到 `100%` 后，应视为完成。
- 到后续日期不应再继续出现。
- 分次 Todo 的删除语义应与普通 Todo 一致：删除后结束整条 Todo。

## 7. 重复 Todo 行为
- 创建一条带重复规则的 Todo。
- 当天应出现当前 Todo。
- 若删除当天实例，则同一天不应再出现该实例。
- 删除当天实例不应停止未来重复。
- 若执行“停止重复”，未来日期不应再出现新的重复 Todo。
- 若当前 occurrence 未完成，到下一个命中日期时，不应出现难理解的多条重复堆叠。
- 完成当前 occurrence 后，才应进入下一次 occurrence。
- 本轮若某些 repeating Todo 仍按旧规则运行，应以 V2.1-4 为后续重构范围记录，而不是在本轮 carryover 中强行修补。

## 7A. V2.1-4 推荐口径
- 同一重复模板在同一时间应最多只有一个 active pending occurrence。
- 若旧 occurrence 未完成，下一次命中日不应再无脑生成新的同类 Todo。
- 旧 occurrence 应通过 carryover 出现在新日期。
- carryover 出来的 repeating Todo 仍属于同一 occurrence，不应被当成新 occurrence。
- 删除当前 repeating occurrence 应表示“结束/跳过本次”，而不是停止整条重复规则。
- 停止重复应保留历史实例，但未来不再生成新的 occurrence。

## 7B. V2.1-4 当前实现验证
- 创建一条 repeating Todo，并保持未完成，切到下一个命中日。
- 不应再额外生成第二条同模板的新 repeating occurrence。
- 旧 occurrence 应以 carryover 形式继续出现。
- 删除 carryover 后，当天刷新不应补回这条 occurrence。
- 删除 carryover 后，后续新的命中日仍应可以再次生成新的 occurrence。
- 完成当前 repeating occurrence 后，后续新的命中日应可以再次生成新的 occurrence。
- “停止重复”后，未来命中日不应再生成新的 occurrence，但历史实例应保留。

## 8. UI 交互一致性
- Todo 列表中的编辑、完成、取消完成、删除按钮，不应因为来源不同而出现难理解差异。
- 手动输入、从种草加入、重复生成、跨日延续后的 Todo，都应保持一致的基础交互直觉。
- 若展示来源标签，它应只作为辅助信息，不应决定按钮是否可用。

## 8A. TodoViewModel 收口
- Todo 列表应继续正常显示手动、种草、重复、延续等轻量标签。
- 编辑按钮显示应基于统一能力判断，不应因为 JSX 中散落的来源分支而失效。
- 删除按钮显示与禁用状态应基于统一能力判断。
- 完成 / 取消完成按钮禁用状态应基于统一能力判断。
- “停止重复”入口应继续只出现在当前支持的重复 Todo 上。

## 9. 种草区边界
- 种草区应继续用于保存“以后有空再做”的内容。
- 用户无需先种草，仍可直接使用 Todo。
- 从种草加入后，用户感知到的是普通 Todo，而不是模板实例管理。

## 10. 构建与 lint
- 后续进入实现阶段后，应执行 `pnpm run build`
- 后续进入实现阶段后，应执行 `pnpm run lint`
- 当前文档轮不要求执行

## 当前明确未覆盖能力
- `decision_selected` 转重复
- 拖动排序 / 拖动改变晚间语境
- 导出 / 导入
- 已归档种草恢复
- 搜索 / 筛选
- 自动化业务测试
- iOS App 迁移
