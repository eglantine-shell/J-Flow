# 约束文档

本文档用于明确当前阶段“不做什么”与“哪些边界不能被悄悄突破”。

说明：
- 若文档之间有冲突，以 `product-rules.md` 为准。
- 当前主线已切换到 `J-Flow V3 Desktop`。
- 本文档描述的是 V3 当前阶段约束，而不是早期 V1 约束。

---

## 一、当前产品边界

### 1. 当前仍是单人使用
- 不做多人协作
- 不做共享空间
- 不做家庭或团队场景

### 2. 当前主线是跨平台 Desktop，macOS 优先开发
- 优先实现 `J-Flow V3 Desktop`
- 优先在 macOS 上开发、自测和试用
- Windows 适配与打包后置到单独里程碑
- 当前不做 iOS 原生 App
- 当前不做 macOS 原生 App
- 当前不做 Android 原生 App

### 3. 当前不继续新增网页端功能
- 网页端保留
- 网页端不删除
- 网页端仍可构建与试用
- 但后续新增功能、修复和 UI 调整默认优先进入桌面版

### 4. 当前不做账号系统
- 不做登录
- 不做用户体系
- 不做账号绑定
- 不做权限管理

### 5. 当前不做云数据库
- 不引入在线后端数据库作为 V3 第一阶段基础
- 不把当前版本重心切到服务端架构

### 6. 当前以本地储存与数据安全为优先
优先确保：
- 本地数据库稳定
- 导入 / 导出可用
- 数据目录清晰
- 自动备份可落地
- schema migration 可控

不优先追求：
- 最小安装包
- 跨设备实时同步
- 云端无缝漫游

---

## 二、明确当前不做的方向

### 1. 不做天气功能
- 顶部天气占位能力废止。
- 后续改为日历跳转入口。
- 不接天气 API。
- 不做定位。
- 不做天气影响推荐逻辑。

### 2. 不做 `J-Flow` 自有云同步体系
当前阶段仍不做：
- `J-Flow` 官方云同步
- `J-Flow` 自建云数据库同步
- 云数据库实时同步
- 多端在线协作式合并

当前阶段允许：
- 本地文件夹手动同步
- 本地文件夹最小自动同步
- `OneDrive` 保留为未来目标，但暂缓

但当前仍不做：
- 实时同步
- 后台定时同步
- `Dropbox / Google Drive / WebDAV` 等其他云目标
 - 文件系统 watcher 驱动的自动同步
 - 用户自定义自动同步策略

说明：
- 当前允许：
  - 托盘 / 菜单栏常驻
- 但这不表示当前已经开放：
  - 托盘 / 菜单栏后台同步系统
  - 后台定时同步
  - watcher 驱动同步

### 3. 当前不做 iCloud 同步
- 不做 iCloud Drive
- 不做 iCloud 数据库存储
- 不做 Apple 生态同步方案

### 4. 当前不做手机端同步
- 不做 iPhone / iPad 同步
- 不做 Android 同步
- 不以移动端打通为当前目标

### 5. 不直接同步运行时数据库文件
- 即使未来支持跨设备同步，也不建议直接同步 SQLite 运行时数据库文件本体。
- 未来更合理的方向是：
  - 每台设备保留自己的本地数据库
  - 用同步目录保存 item / change log / sync metadata

### 5.1 JSON 备份不是同步包
- JSON 备份继续用于：
  - 完整导出
  - 完整导入
  - 自动备份
  - 手动恢复
- JSON 备份不应被当成跨设备同步协议包。
- 当前跨设备同步继续复用：
  - `SyncTargetDriver`
  - `sync-info.json`
  - `devices/`
  - `items/`
  - `tombstones/`
  - `locks/`
  - `LWW`
  - `syncNow`

### 6. 不一上来重写整个项目
- 不因为切到 Desktop 就整体推翻现有 React / Vite 代码。
- 优先做封装、抽象和渐进迁移。

### 7. 当前不转向 Apple 原生 App 路线
- 不引入 Swift
- 不引入 SwiftUI
- 不转向 macOS 原生 App

### 8. 当前不进入正式发布流程
- 不做 App Store
- 不做 iCloud
- 不做 Apple Developer Program 流程
- 不做代码签名
- 不做公证

### 9. 不为桌面版牺牲网页端可运行状态
- 网页端仍需保持可运行、可构建、可参考。
- 不删除 V2 历史文档。
- 不把现有 V2 资产当成一次性原型直接抛弃。

---

## 三、当前实现约束

### 1. 桌面能力必须经受控桥接暴露
- 不让 renderer 直接滥用 Node / 文件系统能力。
- Electron 中优先使用：
  - `main process`
  - `preload`
  - 受控 API bridge

### 2. 本地数据库优先级高于短期迁就
- 若 IndexedDB 继续保留，也应视为过渡方案，而不是 V3 终态。
- V3 第一阶段推荐主库方向为 `SQLite`。

### 3. JSON 只作为交换与备份格式
- JSON 应用于：
  - 完整导出
  - 完整导入
  - 迁移
  - 备份
- 不建议把单个巨大 JSON 当运行时主数据库。

### 4. 规则空白不能自行扩展
- 若发现产品规则空白：
  - 先记录
  - 再补文档
  - 再实现

### 5. 数据安全能力属于桌面版基础能力
`V3 Desktop` 的基础能力必须包含规划，至少文档先明确：
- 数据目录
- 导入 / 导出
- schema version
- migration
- 自动备份
- 打开数据文件夹
- 数据目录、文件读写、数据库访问应尽量采用跨平台实现方式，避免写死 Windows 路径假设。

---

## 四、未来可预留但当前不实现

- 本地文件夹同步之外的更多同步目标
- `OneDrive`
- `Dropbox / Google Drive / NAS` 挂载目录协同
- 模糊搜索与智能联想
- 智能学习
- 提醒与通知
- 更复杂的 recurrence 自定义器
- 多用户 / 协作

说明：
- 当前同步实现只保留本地文件夹手动同步。
- `OneDrive` 保留为未来目标，但当前暂缓。
- 这不表示已经开始实现自动同步，也不表示放开 `Dropbox / Google Drive / WebDAV` 等其他第三方同步目标。
