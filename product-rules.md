# 产品规则文档

本文档定义当前有效的产品规则。

说明：
- 当前主线已切换为 `J-Flow V3 Desktop`
- 若文档之间有冲突，以本文档为准
- 若现有实现与本文档冲突，以本文档作为后续迭代目标

---

## 一、当前阶段定位

### 1. V2 Web 的定位
- 已部署网页端属于 `V2` 阶段结果。
- V2 网页端基本可用，但仍存在业务逻辑与 UI 缺陷。
- 网页端暂不继续增改，但继续保留为：
  - 已部署版本
  - 可试用版本
  - V3 的参考版本

### 2. V3 Desktop 的定位
- `J-Flow V3 Desktop` 是当前主线。
- 后续新增功能、修复和 UI 调整优先进入桌面版。
- 待桌面版试用稳定后，再评估是否回写网页端。

---

## 二、核心原则

### 1. Todo 仍是一等公民
- 用户心智中的主语仍然是 Todo。
- 无论来源如何，只要进入 Todo 列表，就应支持一致的基础交互。

### 2. 种草是 backlog，不是 Todo 模板
- 种草负责收藏。
- Todo 负责执行。
- 从种草加入 Todo 后，本次执行属性在 Todo 侧确定。

### 3. 桌面端优先保证长期自用稳定性
- 先保证：
  - 本地数据稳定
  - 导入 / 导出清晰
  - 桌面交互稳定
- 不在第一阶段追求复杂同步和账号体系。

### 4. 自动备份优先按最小稳定策略落地
- 桌面版应默认启用本地自动备份。
- 自动备份当前使用完整 JSON 快照格式。
- 自动备份当前默认保留最近 `20` 份。
- 第一阶段优先：
  - 自动创建备份
  - 可见备份目录
  - 可手动立即备份
- 第一阶段不要求立刻支持：
  - 自定义计划
  - 自定义保留数量
  - 备份列表管理

### 5. 桌面版允许常驻，但不等于开放后台同步系统
- 桌面版当前允许：
  - 关闭窗口后应用继续常驻
  - 通过托盘 / 状态栏入口重新打开
  - 通过显式操作退出应用
- 这里开放的是：
  - 桌面壳层常驻能力
- 不等于当前已经开放：
  - 后台定时同步
  - 文件系统 watcher
  - 用户自定义自动同步策略
  - 复杂同步中心

---

## 三、Todo 规则

### 1. 普通 Todo
- 普通 Todo 未完成时，仍按既有规则顺延到今天。
- 普通 Todo 删除表示结束这条 Todo。
- 普通 Todo 完成表示这条 Todo 当前结束。

### 2. 完成事项显示规则
- Todo 需要区分：
  - 当前有效计划日期
  - 完成日期
- 未完成事项按“当前有效计划日期”归属显示。
- 已完成事项按“完成日期”归属显示，而不是按原计划日期显示。
- 这里修改的是“列表显示归属”，不等于要重写事项原本的计划字段：
  - 原 `date / originDate / targetDate` 关系应继续保留其计划语义
  - `completedAt` 负责已完成事项的显示归属
- Todo 完成后，应自动移动到完成日期对应页面的最下方。
- 未完成事项保持在上方。
- 已完成事项在下方按 `completedAt` 排序：
  - 完成时间越早越靠上
  - 完成时间越晚越靠下
- 历史已完成事项若缺少 `completedAt`：
  - 仍保留在已完成组
  - 默认排在已完成组最后
- 已完成事项应显示为无语境色。
- 已完成事项仍允许恢复未完成。
- 恢复未完成后，不参与已完成区排序，应回到“当前有效计划日期”的未完成区。
- 若该事项此前已经被顺延到今天，则“当前有效计划日期”视为今天：
  - 完成后再取消完成，应回到今天，而不是误回更早历史日期。
- 恢复未完成后的精确落点，默认可放到未完成列表末尾；若后续实现可保留原顺序信息，则以原顺序恢复为更优。

### 3. 完成时间修改
- 已完成事项应支持修改 `completedAt`。
- 完成时间的静态展示与编辑控件统一使用：
  - `24` 小时制
  - 时间格式为 `HH:mm`
- 编辑完成时间时，不应因为 macOS / Chromium 的本地化行为回退为 `12` 小时制。
- 修改后，已完成列表顺序应立即随之更新。
- 修改后，事项应迁移到 `completedAt` 对应日期的已完成区。
- 修改完成时间不改变事项 `date`。
- 修改完成时间不触发重复规则变化。
- 当前阶段修改 `completedAt` 只影响已完成事项显示归属，不应顺手触发“完成后重复”的下一次生成。

### 4. 白天 / 晚上语境
- 未完成事项继续保留 `day / night` 语境。
- 白天事项颜色需要在 V3 中调得更明显，以便与已完成无色状态区分。
- 晚上事项可继续保留现有方向，但也必须与已完成状态清晰区分。

### 5. 必要事项 DDL
- `DDL` 只属于：
  - 必要事项
- 当 `isNecessary = true` 时：
  - `DDL` 必填
  - 第一版默认取当前事项 `date`
  - 等价于：
    - `1 日内完成`
- 当取消必要时：
  - 自动清空 `DDL`
- 第一版 `DDL` 只支持：
  - 日期
  - 不支持具体时间
- 表单输入层同时支持：
  - 日历点选真实日期
  - `x 日内完成`
- `x` 取值范围：
  - `1-100`
- 一旦保存后，底层以真实日期 `deadlineDate` 为准。
- 修改 Todo 的 `date` 时：
  - 不自动顺延 `DDL`
  - 若新 `date` 已到 `DDL` 当天或更晚，则直接显示今日截止或已逾期
- `DDL` 只能通过显式编辑修改。
- 第一版不做：
  - 提醒
  - 自动排序调整
  - 自动弹窗
- 已完成事项：
  - 不再显示 `DDL`

### 5.1 只看必要
- Todo 页面应提供：
  - `只看必要`
  筛选按钮。
- 按钮位于：
  - `调整顺序`
  按钮左侧。
- 筛选对所有可查看日期生效：
  - 今天
  - 历史日期
  - 未来日期
- 开启后只显示：
  - `isNecessary = true`
  的 Todo。
- 筛选范围同时包括：
  - 未完成必要事项
  - 已完成必要事项
- 再次点击应恢复显示该日期的全部 Todo。
- 开启筛选后，按钮文案改为：
  - `全部事项`
- `只看必要` 与排序模式第一版互斥：
  - 开启 `只看必要` 后，`调整顺序` 显示禁用状态
  - 用户必须先点击 `全部事项` 恢复完整列表
  - 恢复后才可进入排序模式
  - 排序模式中不允许开启 `只看必要`
- 该能力只改变当前页面的显示结果：
  - 不修改 Todo 数据
  - 不修改 `sortOrder`
  - 不改变完成状态
  - 不改变日志快照

### 5.2 V2.4 备注替代准备
- `准备` 不再作为用户需要勾选的 Todo 属性。
- 新增 / 编辑 Todo 时，事项内容输入框下方应提供一个轻量单行备注输入框。
- 备注输入框可以为空。
- 备注不参与 Todo 是否可提交的校验。
- 备注保存后，在未完成 Todo 条目中显示为：
  - `备注：...`
- 旧数据中已有的 `requiresPreparation / preparationNotes` 应兼容为备注显示：
  - 只要 `preparationNotes` 非空，就按备注展示
  - 不再展示为 `前置准备内容：...`
- 新建 Todo 时不再要求用户先选择 `准备` 才能填写备注。
- 从种草加入 Todo 时，备注属于当前 Todo 执行属性，不回写种草模板。

### 5.3 V2.4 夜间新增默认归属
- 设置页应提供“夜间新增 Todo 默认归属晚上”的偏好设置。
- 该设置默认关闭。
- 关闭时：
  - 新增 Todo 默认仍为白天。
- 开启时：
  - 若创建时的本地小时落在用户设置的夜间区间内，新增 Todo 默认选择晚上。
  - 否则默认选择白天。
- 用户应能设置夜间开始小时与结束小时。
- 小时取值范围为：
  - `0-23`
- 默认建议值为：
  - 开始 `17`
  - 结束 `23`
- 区间允许跨午夜，例如：
  - `22-6`
  - 表示 `22:00-23:59` 与 `00:00-05:59`
- 该设置只影响打开新增 Todo 表单时的默认 `timeBlock`。
- 用户在表单内手动切换白天 / 晚上后，应以用户选择为准。
- 编辑已有 Todo 时，不应用该默认规则，应始终读取原 Todo 的 `timeBlock`。

### 5.4 V2.4 分步 Todo
- `分步` 是独立于 `分次` 的 Todo 类型。
- `分次` 表示一个事项的百分比进度。
- `分步` 表示一个事项按步骤推进，完成当前步骤后可自动生成下一步 Todo。
- `分步` 与 `分次` 第一版互斥：
  - 选择分步时应关闭分次
  - 选择分次时应关闭分步
- 新增分步 Todo 时，表单应包含：
  - 事项内容
  - `当前` 步骤输入框
  - `下一步` 步骤输入框
- `当前` 步骤必填。
- `下一步` 步骤可以为空。
- 列表中分步 Todo 的显示标题为：
  - `事项内容：当前步骤`
- 完成分步 Todo 时：
  - 当前 Todo 标记完成
  - 若 `下一步` 非空，系统自动创建一条新的 pending Todo
  - 新 Todo 的事项内容沿用原事项内容
  - 新 Todo 的 `当前` 步骤取上一条的 `下一步`
  - 新 Todo 的 `下一步` 初始为空
  - 新 Todo 不继承上一条的 `deadlineDate`
  - DDL 只属于当前这一步
- 若 `下一步` 为空，完成当前 Todo 后分步链路结束。
- 编辑分步 Todo 时，应能修改：
  - 当前步骤
  - 下一步步骤
- 分步 Todo 的基础交互仍应遵守 Todo 一等公民原则：
  - 可完成
  - 可取消完成
  - 可删除
  - 可编辑
  - 可顺延
- 分步 Todo 的日志第一版可先使用拼接后的显示标题进入快照，不要求新增单独日志分区。

### 5.5 V2.4 初始化页与功能教学
- 初始化页需要承担两类职责：
  - 首次开局配置
  - 核心功能教学
- 首次开局配置继续包括：
  - 种草清单
  - 有空就做场景
- 功能教学应围绕当前有效心智展开：
  - 种草是 backlog，不是 Todo
  - Todo 是执行层
  - 从种草加入 Todo 后，必要 / 备注 / 分步 / 分次 / 重复等执行属性属于当前 Todo
  - Todo 可按白天 / 晚上与日期组织
  - 未完成普通 Todo 会按既有规则顺延
  - 完成事项会进入日志快照
  - 本地数据可通过设置页导入 / 导出 / 备份 / 同步
- 初始化教学不应变成大段说明书，应使用短步骤、可跳读的真实 UI 教学态。
- 功能教学应使用独立演示数据，不直接使用用户当前本地数据：
  - 教学时展示一套新编 demo 数据
  - demo 数据应覆盖各类核心使用场景
  - demo 数据不得写入 SQLite / IndexedDB
  - demo 数据不得覆盖、删除或同步用户真实 Todo、种草、日志、设置、同步目标或自动备份信息
  - 教学结束后应回到真实应用数据
- 功能教学视觉规则：
  - 教学栏应放在真实 UI 之外的底部独立 rail 中，页面内容应为底部 rail 让位
  - 进入桌面端教学态时，应通过受控 Electron bridge 尽量将窗口高度调整到当前屏幕可用最大高度
  - 窗口扩高不应进入全屏，不应改变用户数据，不要求教学结束后自动恢复窗口高度
  - 教学栏不跟随目标漂移
  - 当前被介绍的真实区域应出现高亮框
  - 高亮框内亮度应高于周围，框外区域应明显压暗
  - 高亮框只用于指示，不应遮住被介绍控件
  - 教学栏不应遮挡当前正在介绍的关键 UI；必要时页面内容应滚动到可见区域
- 教学界面必须使用 J-Flow 真实 UI：
  - 使用 demo 数据渲染真实页面、真实组件与真实样式
  - 不使用与实际界面脱节的抽象卡片或独立演示舞台替代教学
  - 教学中的保存、删除、同步、导入、导出等危险动作不得作用于真实数据
- 首次初始化流程完成时：
  - 保存用户对种草清单与场景的配置
  - 将 `settings.initialized` 置为 `true`
  - 进入主界面
- 设置页应提供 `初始化与使用教学` 卡片：
  - 说明文案为：`重置应用后会清空当前本地应用数据，并回到第一次打开应用的初始化流程；也可以在此重看使用教学。`
  - 卡片内提供两个同级按钮：`重置应用`、`使用教学`
- `使用教学` 不应清空当前本地数据。
- `使用教学` 不应重置 Todo、种草、日志、设置、同步目标或自动备份信息。
- `使用教学` 应允许已初始化用户重新经历功能教学。
- 第一版使用 URL 参数启动教学演示模式：
  - `?tutorial=1`
- 教学演示第一版流程固定为：
  - 日期：高亮侧栏 `THIS DAY` 与日历区，说明当前查看日期与今天的不同高亮
  - 首页下方种草入口：不切换到种草清单页，应展开真实种草输入区；说明点击 `+` 收纳有空再做，胶囊 tag 用于筛选，兴趣程度用于排序，同属性内容可批量添加
  - Todo 输入面板：展示 `TODO / 拔草` 切换、拔草条目状态、分次勾选与备注输入区
  - 日夜切换：说明白天 / 晚上语境，以及设置页可开启夜间新增默认归属晚上
  - 必要按钮与 DDL 区：说明必要必须设置 DDL，分步事项的 DDL 只属于当前这一步
  - 分步与分次：说明完成当前步骤自动生成下一步，以及分次进度条记录推进百分比
  - 重复：说明按日历重复与完成后重复的差异，重复周期支持 `1-100` 的日 / 周 / 月 / 年
  - 同步功能：说明本地文件夹同步可借助坚果云、OneDrive、iCloud Drive、Dropbox、NAS 等外部目录交换数据
  - 种草清单、日志和其他设置：切换到种草清单页，同时高亮侧栏下方种草清单 / 日志 / 设置导航区
- `/setup` 仍用于首次初始化。
- 已初始化用户不应被允许误入会保存初始化配置的 `/setup`。
- 未初始化用户仍应被引导到 `/setup`。
- 教学完成后的落点：
  - 首次初始化：进入教学演示模式
  - 重看教学：进入教学演示模式
  - 教学结束或跳过后：回到真实应用数据与真实页面
- 第一版不要求新增持久化字段记录教学完成时间。
- 若后续需要记录教学是否看过，可再设计设置字段；不得在本轮暗中加入未使用字段。

### 5.6 工作日 Todo 暂缓
- 工作日 Todo 暂不进入 V2.4 实现范围。
- 原因：
  - 若不能可靠导入或维护节假日 / 调休日历，仅跳过周末的产品意义不足。
- 后续若重启该方向，需要先定义：
  - 节假日数据来源
  - 调休工作日处理
  - 用户自定义日历或导入方案
  - 顺延与手动改期的边界

### 6. 日志快照展示
- 日志页与复制出的 Markdown 当前统一为：
  - `### 当日快照`
  单一列表
- 不再分为：
  - 当日完成
  - 当日未完成
  - 当日删除
- 日志条目统一使用：
  - `- [x]`
  - `- [ ]`
  作为前缀
- 已删除事项在日志中显示为：
  - `- [x] ~~事项~~`
- 必要事项在日志中继续加粗显示。
- `[拔草]`
  - `[逾期]`
  - `[分次]`
  作为末尾可叠加 tag
- 已完成必要事项：
  - 未逾期 / 当天完成时不显示 `DDL`
  - 仅在逾期完成时显示：
    - `[逾期]`
- 未完成必要事项：
  - 显示：
    - `DDL MMDD`
  - 若已逾期，再追加：
    - `[逾期]`
- 分次事项：
  - 当日有推进时显示：
    - `已推进 a%→b%`
  - 当日无推进时显示：
    - `当前进度 b%`

---

## 四、重复规则

### 1. 重复规则升级目标
旧版仅有：
- 不重复
- 每天
- 每周
- 每月
- 每年

V3 中需要升级为两大类。

### 2. A 类：日历式重复
含义：
- 无论当前事项是否完成，下一次都按固定周期定期出现。

支持：
- 每 x 天
- 每 x 周
- 每 x 月
- 每 x 年

规则：
- `x` 取值范围为 `1-100`
- 这类重复继续按命中日生成新的 occurrence
- 当前未完成，不阻止未来命中日继续出现新的 occurrence

### 3. B 类：完成后重复
含义：
- 只有当前事项完成后，才根据完成时间生成下一次。

支持：
- 完成后 x 天
- 完成后 x 周
- 完成后 x 月
- 完成后 x 年

规则：
- `x` 取值范围为 `1-100`
- 若当前事项未完成，则下一次不会出现
- 下一次生成基准是 `completedAt`，不是原 `date`
- 本阶段采用更稳定的最小策略：
  - 若系统已经根据某次完成生成了下一次 pending occurrence
  - 后续再修改这次完成的 `completedAt`
  - 不自动追溯调整已生成的下一次日期
  - 用户后续以新 occurrence 为准继续流转

### 3.1 停止重复与恢复重复
- 停止重复时：
  - 保留当前日期及以前已经存在的 occurrence
  - 清理当前日期之后的 future occurrence
- 这里的“未来”以 occurrence 的目标日期判断，而不是以创建时间判断。
- 清理 future occurrence 时，边界应使用用户当前操作的目标日期：
  - 即当前 `selectedDate / targetDate`
  - 不是系统今天日期
- 恢复重复时：
  - 不要求立即回填之前被清理的未来条目
  - 继续沿用“切到目标日期时再生成”的懒生成策略
- 这样做的原因是：
  - 与当前系统的“进入某天时同步生成”机制一致
  - 风险更低
  - 用户侧体验差异很小

### 4. 新模型必须能区分三类状态
- `none`
- `calendar`
- `afterCompletion`

### 5. 月 / 年重复遇到不存在日期
- 若锚点日期不存在，例如 `1 月 31 日` 每月重复：
  - 建议规则为“落到目标月最后一天”
- 年重复同理：
  - 例如 `2 月 29 日` 非闰年时，落到 `2 月 28 日`

### 6. 日期计算规则
- `day`
  - 加 `x` 天
- `week`
  - 加 `7 * x` 天
- `month`
  - 按日历月加 `x` 月
  - 若目标月不存在该日，落到目标月最后一天
- `year`
  - 按日历年加 `x` 年
  - 若目标年对应月份不存在该日，落到该月最后一天

### 7. 旧规则迁移
- 旧 `daily / weekly / monthly / yearly` 全部迁移为：
  - `repeatType = calendar`
  - 对应 `intervalUnit`
  - `intervalValue = 1`

### 8. UI 表达
- 不再使用五个单选小标签作为最终形态。
- 推荐改为：
  - 重复类型分段选择
  - 数字输入
  - 单位选择

---

## 五、Todo 手动排序

### 1. 排序能力
- Todo 需要支持手动排序。
- 第一阶段若拖拽复杂，可先做：
  - 进入排序模式
  - 上移 / 下移按钮
- 当前阶段性实现采用：
  - 排序模式
  - 上移 / 下移按钮
  - 每次移动即时保存
- `V2.3.2` 目标升级为：
  - 排序模式
  - 通过拖动把手调整顺序
  - 拖动结束后保存
- 不要求用户直接拖动整张事项卡片，避免与：
  - 编辑
  - 完成
  - 删除
  冲突。
- 拖动排序稳定后移除上移 / 下移按钮。

### 2. 白天 / 晚上分隔线
- 排序页面中显示白天 / 晚上的实线分隔线。
- 非排序页面中不显示这条实线。

### 3. 越过分隔线时的行为
- 当事项向上或向下越过白天 / 晚上分隔线后：
  - `timeBlock` 应同步更新
  - 颜色语境应同步变化
  - 该变化只作用于当前日期实例，不要求同步改模板全局语义
- 跨分隔线移动不应把另一条相邻 item 一起挤到对侧。
- 正确语义是：
  - 移动中的 item 从原组移出
  - 插入目标组边界位置
  - 只有移动中的 item 改变 day/night 归属
- 同组内移动才是相邻交换；跨组移动不是双向对调归属。

### 4. 排序作用范围
- 手动排序默认只影响当天实例。
- 已完成事项不参与未完成事项排序。
- 已完成事项继续：
  - 显示在底部
  - 按 `completedAt` 排序
  - 不受 `sortOrder` 影响

### 4.1 当前排序字段
- 当前优先复用 `DayPlanItem.sortOrder`
- 不新增独立 `order` 字段
- `sortOrder` 表示当前日期实例的未完成排序位置
- 第一版可按 `1, 2, 3...` 重新编号

### 5. 与重复事项的关系
- 当天重复事项实例可参与当天排序。
- 下一次重复出现是否继承排序位置：
  - 默认不强依赖继承
  - 若有稳定可解释策略，可参考原模板或同来源最近一次排序
  - 但不能影响当天实例排序优先级

### 6. 与自动生成的关系
- 自动生成只负责决定事项出现。
- 一旦事项已出现在当天列表，用户手动排序优先于自动默认顺序。

### 7. 拖动排序交互
- 拖动排序只处理：
  - 当前日期
  - 未完成事项
- 已完成事项不进入拖动区域。
- 拖动结束后应统一保存：
  - 当前未完成事项的新 `sortOrder`
  - 跨越日夜分隔线时的新 `timeBlock`
  - 与新 `timeBlock` 一致的 `timeBlockSource`
- 拖动过程中应有明确的：
  - 拖动项状态
  - 可放置位置提示
- Todo 列表可滚动时，拖动排序应支持在列表内继续滚动。
- 应提供键盘可操作的排序能力，避免排序只能依赖鼠标。
- `只看必要` 与排序模式第一版互斥，避免隐藏的非必要事项发生不可解释的顺序变化。

---

## 六、种草规则

### 1. 主页面保留种草区
- 主页面底部继续保留“种草区”。
- 种草区继续承担：
  - 输入
  - 保存
  - 轻量展开 / 收起

### 2. 种草清单独立页面
- “种草清单”不再在主页底部长列表展开。
- 已保存种草项的完整浏览区迁移为独立页面。
- 该页面与设置页平级。

### 3. 主页与种草清单的边界
- 主页底部种草区只负责轻量输入与保存。
- 独立种草清单页面负责：
  - 查看
  - 筛选
  - 编辑
  - 删除
  - 归档

### 3.1 种草清单展示范围
- 种草清单页面不再只显示 `active` 种草。
- 应显示“所有未完成种草”，包括：
  - 尚未加入 Todo 的种草
  - 已加入 Todo 但对应 Todo 尚未完成的种草
- `picked` 状态的种草，只有在仍存在未完成 Todo 时才继续显示。
- `archived` 种草不显示在当前种草清单中。

### 4. Todo 与种草的关系
- Todo 页面仍应可以：
  - 从种草中选择
  - 推荐拔草
- 主页种草区保存的新种草项，应立即出现在独立种草清单页面数据中。
- 从种草加入 Todo 后，该种草进入 `picked` 状态，不继续作为拔草候选。
- V2.4 起，来自种草的一次性 Todo 删除时，不再自动恢复原种草到活动库。
- V2.4 起，来自种草的一次性 Todo 应提供独立的“回到种草清单”动作：
  - 该动作将当前 Todo 标记为删除
  - 同时将对应种草恢复为 `active`
  - 该动作只对未完成的拔草 Todo 显示
  - 该动作不需要二次确认
  - 该动作不需要额外成功反馈
- 完成来自种草的一次性 Todo 后，对应种草不应回到活动库。
- 删除来自种草的一次性 Todo 后，对应种草保持 `picked`，除非用户显式点击“回到种草清单”。
- 彻底删除拔草 Todo 前应有二次确认。

### 4.1 种草清单页的 “TODO” 按钮规则
- 种草清单页中，每条未完成种草都应提供一个轻量化 `TODO` 按钮。
- 默认目标是：
  - 真实今日
  - 白天
- 若该种草尚未加入任何未完成 Todo：
  - 点击后将其作为普通事项加入今日白天
  - 默认属性为：
    - 非必要
    - 非重复
    - 无备注
    - 不分次
- 若该种草已经在今日白天或今日晚上存在未完成 Todo：
  - `TODO` 按钮不可点击
- 若该种草已经排入其他日期的未完成 Todo：
  - 点击 `TODO` 按钮时，不新建副本
  - 直接将原未完成 Todo 移入真实今日白天
- 已排入未完成 Todo 的种草，应在条目上显示小标签：
  - `已排在 M/D`

---

## 七、批量种草规则

### 1. 输入位置
- 批量种草发生在主页底部种草区输入框中。

### 2. 交互规则
- 支持分行输入多条内容。
- 每行代表一条独立种草项。
- 多行共享同一组元信息：
  - 种草分类 / 清单
  - 时间场景
  - 兴趣程度
- 当前第一版采用稳定实现：
  - 多行 `textarea`
  - 按换行切分生成多条
  - 不做复杂动态占位行交互

### 3. 数量限制
- 最多支持 `20` 行。
- 空行自动忽略。
- 超过 `20` 行时应阻止继续输入或给出提示。

### 4. 保存结果
- 提交后生成 `1-20` 条独立种草项。
- 若只有 1 行有效内容，行为应与原单条添加一致。
- 不允许把多条内容存成一个 item。
- 后续这些 item 应可分别：
  - 编辑
  - 归档
  - 拔草

---

## 八、本地储存与未来同步方向

### 1. V3.0
- 本地桌面版
- 本地数据库
- 导入 / 导出

### 2. V3.1
- 自动备份
- 数据目录管理
- 恢复能力

### 3. V3.2 或更后
- 本地文件夹同步

### 4. 当前不是目标
- 云同步
- 手机端同步
- iCloud 同步
- 账号系统
